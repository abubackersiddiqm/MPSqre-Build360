# MPSqre Build360 v0.19.1

## Scope

Phase 19 adoption-snapshot reverse-accessor compatibility hotfix.

## Corrective changes

- Isolates the customer-success `AdoptionSnapshot.company` reverse accessor as `customer_success_adoption_snapshots`.
- Synchronizes the unapplied `successops.0001_initial` migration state with the model contract.
- Adds a Django system-check contract and regression test preventing future reverse-accessor collisions with pilot operations.
- Preserves all Phase 19 billing, support SLA, adoption and customer-success behavior.

## Database impact

No additional migration is introduced. Apply this hotfix before the first Phase 19 migration. The corrected `successops.0001_initial` migration creates the same database foreign key while using a unique Django reverse relation name.
