from django.apps import AppConfig


class AccessOpsConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "modules.accessops"
    verbose_name = "Platform access and onboarding"
