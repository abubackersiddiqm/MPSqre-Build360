from django.apps import AppConfig


class SustainabilityOpsConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "modules.sustainabilityops"
    verbose_name = "Build360 Sustainability, ESG, Carbon and Resource Operations"
